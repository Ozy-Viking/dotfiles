# See https://git-scm.com/docs/git-config

[alias]
	co = checkout
	br = branch
	ci = commit
	st = status
	zip = "!f() { root=$(git rev-parse --show-toplevel) || exit; name=$(basename \"$root\"); out=\"$(dirname \"$root\")/$name.zip\"; rm -f \"$out\"; git -C \"$root\" ls-files -co --exclude-standard | (cd \"$root\" && zip \"$out\" -@); }; f"
	zip-all = "!f() { root=$(git rev-parse --show-toplevel) || exit; name=$(basename \"$root\"); out=\"$(dirname \"$root\")/$name.zip\"; rm -f \"$out\"; git -C \"$root\" ls-files -co --exclude-standard | (cd \"$root\" && zip \"$out\" -@); (cd \"$root\" && zip -r \"$out\" .git); }; f"
[init]
	defaultBranch = main
[pull]
	rebase = true 			 # Rebase (instead of merge) on pull
[push]
	autoSetupRemote = true   # Automatically set upstream branch on push
[diff]
	algorithm = histogram    # Clearer diffs on moved/edited lines
	colorMoved = default
	mnemonicPrefix = true    # More intuitive refs in diff output
[commit]
	verbose = true           # Include diff comment in commit message template
	gpgsign = true
[column]
	ui = auto 			     # Output in columns when possible
[branch]
	sort = -committerdate    # Sort branches by most recent commit first
[tag]
	sort = -version:refname  # Sort version numbers as you would expect
[rerere]
	enabled = true           # Record and reuse conflict resolutions
	autoupdate = true        # Apply stored conflict resolutions automatically
[user]
	name = Ozy-Viking
	email = admin@hankin.io
	signingkey = ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIOkK2XR/BtsVjvEYPap+VUttgWOntP4EZRKclX7iKU5E
[gpg]
	format = ssh
[gpg "ssh"]
	program = /opt/1Password/op-ssh-sign
[safe]
	directory = /home/zack/.sshfs/nas/apps/huly
	directory = /home/zack/.sshfs/nas_mnt_SSDs/apps/huly
	directory = /home/zack/.sshfs/nas_mnt_SSDs/apps/OTEL
[coderabbit]
	machineId = cli/3baa40affab44bb8be5ff2f8d7df1507
[credential "https://github.com"]
	helper = 
	helper = !/usr/bin/gh auth git-credential
[credential "https://gist.github.com"]
	helper = 
	helper = !/usr/bin/gh auth git-credential
[core]
	pager = hunk pager
[interactive]
	diffFilter =  hunk diff
[merge]
	conflictstyle = zdiff3
[color]
	ui = auto
