[[ -r "$HOME/.config/shell/env.sh" ]] && source "$HOME/.config/shell/env.sh"
[[ -r "$HOME/.bashrc" ]] && source "$HOME/.bashrc"

# Vite+ bin (https://viteplus.dev)
if [ -r "$HOME/.config/vite-plus/env" ]; then
  . "$HOME/.config/vite-plus/env"
fi
